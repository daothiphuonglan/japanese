generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model hiragana {
  id     String @id @default(uuid())
  char   String
  romaji String @unique
  type   String
}